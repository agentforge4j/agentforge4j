{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AgentDefinition",
  "type": "object",
  "required": [
    "id",
    "name",
    "locality",
    "version",
    "providerPreferences"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique agent identifier. Must match the agent directory name stem."
    },
    "name": {
      "type": "string"
    },
    "locality": {
      "type": "string",
      "enum": [
        "LOCAL",
        "CLOUD"
      ],
      "description": "LOCAL runs on operator-controlled infrastructure(i.e. Ollama, vllm). CLOUD calls an external API(i.e. ChatGpt, Claude)."
    },
    "enabled": {
      "type": "boolean",
      "default": true,
      "description": "Whether the agent is enabled. Disabled agents are ignored by the system."
    },
    "systemPrompt": {
      "type": "string",
      "description": "Inline system prompt. If omitted, systemprompt.md in the agent directory is used."
    },
    "providerPreferences": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/ProviderPreference"
      },
      "description": "Ordered list of provider preferences. First available provider is used."
    },
    "supportedCommands": {
      "type": ["array", "null"],
      "items": {
        "type": "string",
        "enum": [
          "CREATE_FILE",
          "SET_CONTEXT",
          "USER_PROMPT",
          "RUN_COMMAND",
          "COMPLETE",
          "CONTINUE",
          "GENERATE_QUESTIONS",
          "ESCALATE",
          "TOOL_INVOCATION",
          "REQUEST_CONTEXT"
        ]
      },
      "description": "Subset of commands this agent may return. Null means all commands."
    },
    "author": {
      "type": "string",
      "description": "Author of the agent. Optional metadata."
    },
    "contact": {
      "type": "string",
      "description": "Contact information for the agent author. Optional metadata."
    },
    "version": {
      "type": "string",
      "description": "Agent version. Can be used for display or to trigger specific logic based on version."
    },
    "modelTier": {
      "type": ["string", "null"],
      "enum": [
        "LITE",
        "STANDARD",
        "POWERFUL",
        "PREMIUM",
        null
      ],
      "description": "Optional capability tier resolved to a concrete model per provider at invocation time. A raw model pin on a providerPreference always wins over the tier. Null relies on pins or provider defaults."
    },
    "outputContract": {
      "$ref": "#/$defs/OutputContract",
      "description": "Optional declaration of the structured output this agent is expected to produce."
    }
  },
  "$defs": {
    "OutputContract": {
      "type": "object",
      "additionalProperties": false,
      "required": ["discipline"],
      "properties": {
        "schemaRef": { "type": ["string", "null"] },
        "discipline": { "type": "string", "enum": ["STRUCTURED_ONLY", "STRUCTURED_PREFERRED", "FREEFORM"] },
        "rationaleAllowed": { "type": "boolean" }
      },
      "allOf": [
        {
          "if": { "properties": { "discipline": { "const": "STRUCTURED_ONLY" } } },
          "then": { "required": ["schemaRef"], "properties": { "schemaRef": { "type": "string", "minLength": 1 } } }
        }
      ]
    },
    "ProviderPreference": {
      "type": "object",
      "required": [
        "provider"
      ],
      "additionalProperties": false,
      "properties": {
        "provider": {
          "type": "string",
          "enum": [
            "azure-openai",
            "bedrock",
            "claude",
            "gemini",
            "mistral",
            "ollama",
            "openai",
            "openai-compatible",
            "vllm"
          ],
          "description": "Lowercase provider name."
        },
        "model": {
          "type": ["string", "null"],
          "description": "Optional model override. Null means use provider default."
        }
      }
    }
  }
}
