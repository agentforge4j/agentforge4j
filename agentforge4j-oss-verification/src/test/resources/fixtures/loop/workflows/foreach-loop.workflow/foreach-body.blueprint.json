{
  "kind": "BLUEPRINT",
  "blueprintId": "foreach-body",
  "name": "For Each Body",
  "behaviour": {
    "loopConfig": {
      "terminationStrategy": "FOR_EACH",
      "forEachContextKey": "items",
      "maxIterations": 10,
      "maxIterationsAction": "AWAIT_USER"
    },
    "transition": "AUTO"
  },
  "steps": [
    {
      "kind": "STEP",
      "stepId": "body",
      "name": "Loop Body",
      "behaviour": {
        "type": "AGENT",
        "agentRef": "loop-agent",
        "transition": "AUTO"
      }
    }
  ]
}
