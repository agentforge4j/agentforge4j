# Build-time derivatives, regenerated by this module's own pre-* npm scripts — never hand-edited,
# never committed:
#   catalogue-data.json  <- agentforge4j-workflows-catalog, via scripts/build-catalogue-data.mjs
#   docs-entry.json      <- agentforge4j-docs' version lifecycle, via scripts/build-docs-entry.mjs
src/generated/

# TypeScript incremental-build cache — mutates on every tsc run, pure local build state
*.tsbuildinfo
