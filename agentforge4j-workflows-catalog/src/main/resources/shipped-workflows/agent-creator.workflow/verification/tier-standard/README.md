# Tier resolution — STANDARD

Proves the deterministic `[4]` tier-resolution tree: `complexity=moderate`, `risk=moderate`,
`sensitivityFloor=NONE` resolves to `recommendedTier=STANDARD` via rule
`COMPLEXITY_RISK_MATRIX`, suspending at the `[7]` approval gate.
