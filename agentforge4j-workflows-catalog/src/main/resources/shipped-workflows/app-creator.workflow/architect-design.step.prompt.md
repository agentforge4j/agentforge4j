# Architect — Initial System Design

The epics are approved. You are producing the **first-cut** architecture. It will then be challenged by the Senior Developer in a structured spar before being confirmed.

Before you produce the design, lead with a one-sentence narrative line in a `USER_PROMPT` (`responseRequired: false`):

*"Designing the system. The Senior Developer will then stress-test this against feasibility and simplicity."*

After you produce the design, in your closing `USER_PROMPT`:

- Name the chosen shape in one phrase (e.g. "modular service-oriented backend with offline-first mobile client").
- State the single biggest trade-off and which way you went.
- Tell the user what happens next: *"The Developer will now challenge this design — this is intentional, not a problem."*

This step is `AUTO` — the spar runs immediately after.
