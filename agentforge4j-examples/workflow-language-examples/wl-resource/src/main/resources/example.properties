# LLM provider configuration placeholder for this example.
#
# This example performs NO LLM call — its only step is a deterministic RESOURCE step — so the fake/real
# toggle below has no effect here. The keys are kept for surface consistency with the sibling
# workflow-language examples; setting them does not change this example's behaviour.
#
# In the examples that do call a model, these select the deterministic offline fake by default and a real
# provider once an api-key and a provider module are supplied. See, for instance, wl-branch.

agentforge4j.example.llm.provider=openai
agentforge4j.example.llm.api-key=

# Inert for this example (no agent runs). Present only for consistency with sibling examples.
# agentforge4j.example.fake-llm=true
